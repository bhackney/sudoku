FROM nginx:alpine

# Copy site config
COPY --chmod=644 nginx.conf /etc/nginx/conf.d/default.conf

# Copy the HTML file into the image — avoids host permission issues
COPY --chmod=644 sudoku.html /usr/share/nginx/html/sudoku.html
